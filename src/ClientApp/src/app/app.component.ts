import { Component } from '@angular/core';
/**
 * @example The base component of the app that contains the navbar, tile column, loader component, and router outlet.
 */
@Component({
    selector: 'app-root',
    templateUrl: './app.component.html',
    styleUrls: ['./app.component.scss']
})
export class AppComponent {}
